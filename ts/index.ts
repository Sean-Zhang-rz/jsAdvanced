const fn = (a: any): boolean => {
  // if (typeof a === 'string') {
  //   a
  // } else if (a instanceof Date) {
  //   a
  // }
  a.toString();
  a.toFixed(2);
  return true;
};
const fn1 = (a: unknown): boolean => {
  // if (typeof a === 'string') {
  //   a
  // } else if (a instanceof Date) {
  //   a
  // }
  // a.toString()

  return true;
};
export {};
